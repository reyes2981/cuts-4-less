<h1> Upcoming Appointments </h1>



<% @appointments.each do |appointment| %>
    Appointment confirmed with <%= appointment.hairdresser.name if appointment.hairdresser %> <%= appointment.datetime.strftime("On %m/%d/%Y") %> <%= appointment.datetime.strftime("at %I:%M%p") %></p>
        <p><%= link_to "Appointment Confirmation", appointment_path(appointment) %></p>
<%end%>

 <%= link_to "delete", customer_appointment_path(current_customer.id) %>

  